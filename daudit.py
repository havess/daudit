class Daudit:
    def __init__(self, jobs):
        self._jobs = jobs

    def run(self):
        for job in self._jobs:
            # Async spawn auditer with job
            x = 4

        # Wait for some job to return
        # get result from job
        # send_result to app
        y = 4
